# NAS 배포용 설정

spring:
  datasource:
    url: jdbc:mysql://125.141.20.218:3306/my_new_project_db?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: hyunchang88
    password: gusckd88!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: false
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: nas

server:
  port: 3210

logging:
  level:
    org.springframework.web: INFO
    org.hibernate.SQL: INFO
